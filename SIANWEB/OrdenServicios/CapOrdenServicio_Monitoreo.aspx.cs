using System;
using SIANWEB.Core.UI;

namespace SIANWEB
{
    public partial class CapOrdenServicio_Monitoreo : BaseServerPage
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            if (!Page.IsPostBack)
            {
                // Inicialización server-side futura si se requiere
            }
        }
    }
}
